on:
  push:
    branches:
      - main
    tags:
      - "v*"

name: Release policy

jobs:
  test:
    name: run tests and linters
    uses: kubewarden/github-actions/.github/workflows/reusable-test-policy-go.yml@63e6bff6226bbdd84a4244892417eb27676b7a8c # v5.0.1

  release:
    needs: test
    permissions:
      # Required to create GH releases
      contents: write
      # Required to push to GHCR
      packages: write
      # Required by cosign keyless signing
      id-token: write

    uses: kubewarden/github-actions/.github/workflows/reusable-release-policy-go.yml@63e6bff6226bbdd84a4244892417eb27676b7a8c # v5.0.1
    with:
      oci-target: ghcr.io/${{ github.repository_owner }}/policies/policy-name #FIXME
