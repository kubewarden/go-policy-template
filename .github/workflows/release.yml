on:
  push:
    branches:
      - main
    tags:
      - "v*"

name: Release policy

jobs:
  test:
    name: run tests and linters
    uses: kubewarden/github-actions/.github/workflows/reusable-test-policy-go.yml@ff49d7af0447a59fe9a58a82805713e1a25539f2 # v4.6.2

  release:
    needs: test
    permissions:
      # Required to create GH releases
      contents: write
      # Required to push to GHCR
      packages: write
      # Required by cosign keyless signing
      id-token: write

    uses: kubewarden/github-actions/.github/workflows/reusable-release-policy-go.yml@ff49d7af0447a59fe9a58a82805713e1a25539f2 # v4.6.2
    with:
      oci-target: ghcr.io/${{ github.repository_owner }}/policies/policy-name #FIXME
