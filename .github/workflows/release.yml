on:
  push:
    branches:
      - main
    tags:
      - "v*"

name: Release policy

jobs:
  test:
    name: run tests and linters
    uses: kubewarden/github-actions/.github/workflows/reusable-test-policy-go.yml@0f1672d63bd3572f204917d68390d87f7430069a # v5.0.0

  release:
    needs: test
    permissions:
      # Required to create GH releases
      contents: write
      # Required to push to GHCR
      packages: write
      # Required by cosign keyless signing
      id-token: write

    uses: kubewarden/github-actions/.github/workflows/reusable-release-policy-go.yml@0f1672d63bd3572f204917d68390d87f7430069a # v5.0.0
    with:
      oci-target: ghcr.io/${{ github.repository_owner }}/policies/policy-name #FIXME
